[project]
name = "rag-doc-qa"
version = "0.1.0"
description = "Retrieval-Augmented Generation over your documents: FastAPI, pluggable embeddings + vector store, grounded answers with citations."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
